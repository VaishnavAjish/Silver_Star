const jwt=require('jsonwebtoken'); const token = jwt.sign({id:1,role:'admin'},'change-this-to-a-random-64-char-secret-string',{expiresIn:'7d'}); (async () => { const r = await fetch('http://localhost:5001/api/depreciation-runs', { headers: { Authorization: 'Bearer ' + token } }); console.log(r.status, await r.text()); })();
