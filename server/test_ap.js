const jwt=require('jsonwebtoken'); require('dotenv').config(); const t = jwt.sign({id:1,role:'admin'}, process.env.JWT_SECRET, {expiresIn:'7d'}); (async () => { try { const r = await fetch('http://localhost:5001/api/dashboard/widget/accounts_payable', {headers:{Authorization:'Bearer '+t}}); console.log(r.status, await r.text()); } catch (e) { console.error('Fetch error:', e); } })();
